<!DOCTYPE html>
<html>
<head>
<meta name="viewport" content="width=device-width, initial-scale=1">
<style>
.card {
    box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
    transition: 0.3s;
    width: 100%;
}

.card:hover {
    box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
}

.container {
    padding: 2px 16px;
}
</style>
</head>
<body>

    <h2>&nbsp Feedbacks</h2>
    <% @feedback.each do |feedback| %>
    <div class="col-md-4"> 
    <div class="card" >
    <div class="container">
    <h4><b> <%= feedback.name %></b></h4> 
    <p><label for="Name">Email: </label> <%= feedback.email %></p>
    <p><label for="Name">Subject: </label> <%= feedback.subject %></p>
    <p><label for="Name">Message: </label> <%= feedback.message %></p>
  </div>
</div>
</div>
<% end %>
</body>
</html> 

  
 
</html>
